## 2026-06-07

### test_tts_verify — TTS Reader 创建完成

从 test005 (Voice Reader) fork，重写状态机和 UI：

**功能实现：**
- 4 键操作：KEY1 文件轮询、KEY2 音量循环增减、KEY3 语速循环增减、KEY4 确认/播放/暂停
- 4 屏状态机：FILE_SELECT → READY → PLAYING → PAUSED
- 去掉 USB MSC 模式（无 U 盘功能）
- TTS 引擎：xiaole 语音（esp-sr v2.4.6），音量 5 档(-30/-18/-12/-6/0 dB)，语速 6 档(0~5)
- LVGL UI 重写：4 套独立界面，文件列表、进度条、音量/语速指示

**修改文件：**
- main/CMakeLists.txt：去掉 tusb_msc、tinyusb、usb 依赖
- main/app_tts.h/c：新增 speed 控制、get_volume/get_speed 接口
- main/app_display.h/c：完全重写，6 种显示模式、文件列表 UI、进度条
- main/main.c：完全重写状态机，4 状态 + 按键分发
- 根 CMakeLists.txt、sdkconfig.defaults、partitions.csv、build.sh（新建）
- 未修改：board_pins.h、sd_card.c、es8388.c、xl9555.c、lcd_st7796.c 等硬件层

**编译说明：**
macOS 沙箱代码签名冲突（WorkBuddy Team ID vs pip .so 文件签名不一致），写了 build.sh 供终端手动编译。
